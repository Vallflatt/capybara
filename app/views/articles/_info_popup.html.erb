
<img src="<%= cl_image_path article.photo.key %>" class="article-img">

<div class="infos">
  <div class="article-name" style="font-size: 12px;font-weight: bold;">
  <%= article.name %>
  </div>
  <div>Article porposé par Capy <%= article.user.username %></div>
  <div><%= article.address %></div>
</div>

<%= link_to "<button class='booking-btn rounded-4'>Emprunter</button>".html_safe, new_article_booking_path(article.id), :class=>"booking-link" %>

<style>
  .booking-btn {
    width: 100%;
    background: black;
    padding: 1rem;
    color: white;
    font-weight: bold;

  }

  .article-img {
    width: 100%;
    max-width: 10rem;
    margin: 0 auto;
    display: block;
  }

  .article-name {
    margin-bottom: 0.2rem;

  }

  .infos {
    margin: 0.5rem 0;
    font-size: 14px;
  }
</style>
